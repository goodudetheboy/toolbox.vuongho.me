# 2026-09-25

## Homepage redesign: 3D garage toolbox

- Replaced the card-list homepage with a three.js scene: garage (concrete
  floor, block walls, pegboard with hanging tools, paint-can shelf, LED
  shop light), butcher-block workbench, and a red powder-coated toolbox.
  Clicking the box swings the lid open (physical hinge sim with bounce off
  the stay strap) and flies the camera in. Inside on a foam insert:
  marker → PDF Redactor, microphone → Audio Transcriber, compass → Trip
  Planner (its needle swings to world north), plus loose screwdriver/nuts.
  Hover shows a card; click lifts the tool to the camera and navigates;
  touch uses tap-to-preview, tap-again-to-open. Tools can be dragged and
  tossed (cannon-es). Esc / "Close lid" / tapping the lid puts things back
  and shuts it.
- New deps in `apps/homepage`: `three`, `cannon-es`, `@types/three`.
- Decision recorded in [ADR 0006](adr/0006-homepage-3d-garage-toolbox.md).
- Verified in dev (open/hover/drag/close/launch, mobile 375×812 framing)
  and in the production build via `firebase-tools serve` (lazy chunk
  loads, renders, click opens, no console errors).

Open TODOs:
- Adaptive quality step-down couldn't be observed live (browser pane was
  software-rendered/hidden); worth a glance on a real low-end phone.
- `.claude/launch.json` says homepage runs on 5174, but its Vite config
  has no port so it actually lands on 5173 when nothing else is running.

## Homepage: fix held objects spinning wildly

- Picking up a prop made it whirl: the drag used a stiff point-to-point
  constraint with nothing controlling rotation, so the instant yank
  scraped thin props (marker, nuts — almost no rotational inertia) along
  the box walls/pile and friction spun them at 40–170 rad/s.
- Now held objects are driven like a hand: velocity toward the cursor
  (capped), lift clear of the rim before moving sideways, orientation held
  at the pickup pose with a gentle swing into the direction of travel,
  and `fixedRotation` while held so contacts can't inject spin. Loose
  props are also capped at 18 rad/s after drops.
- Measured after the fix: ≤3.5 rad/s and ≤17° tilt while dragging every
  prop through the same rough path.
